// CAPITULO DOIS

import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import C3 from './C3';
// import registerServiceWorker from './registerServiceWorker';

ReactDOM.render(<C3 />, document.getElementById('root'));

if (module.hot) {
  module.hot.accept();
}

// NÃO COPIEI ESTE ARQUIVO NÃO SEI PRA QUE SERVE.
// registerServiceWorker();

// CAPITULO DOIS